# campaigns-mcp-server – MCP server que expõe o download de peças do campaigns-service

# URL do campaigns-service (Docker: http://campaigns-service:8003)
CAMPAIGNS_SERVICE_URL=http://campaigns-service:8003

# Porta do servidor MCP (Streamable HTTP)
PORT=8010

# Serviço chama o campaigns-service com esses headers (identity interna).
# O campaigns-service exige X-User-* para o GET .../content.
MCP_SERVICE_USER_ID=svc-mcp-campaigns
MCP_SERVICE_USER_EMAIL=mcp-campaigns@internal
MCP_SERVICE_USER_ROLE=Analista de criação
MCP_SERVICE_USER_IS_ACTIVE=true

# Timeout (segundos) para chamadas HTTP ao campaigns-service
HTTP_TIMEOUT=30
